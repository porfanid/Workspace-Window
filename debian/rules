#!/usr/bin/make -f

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@

override_dh_auto_clean:
	# Add custom clean commands here if needed
	dh_auto_clean

override_dh_auto_build:
	dh_auto_build

override_dh_auto_install:
	dh_auto_install
